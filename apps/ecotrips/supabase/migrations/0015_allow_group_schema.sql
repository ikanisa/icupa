-- Unable to update pgrst configuration in this environment; manual configuration may be required
-- to expose the "group" schema over PostgREST. This migration is kept as a placeholder so it can
-- be safely marked as applied without altering database settings.
select 1;
