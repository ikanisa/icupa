// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';
import { getTableSessionHeader } from '@/lib/table-session';

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL;
const SUPABASE_ANON_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY;

if (!SUPABASE_URL) {
  throw new Error('VITE_SUPABASE_URL is not set. Please provide it in your environment configuration.');
}

if (!SUPABASE_ANON_KEY) {
  throw new Error('VITE_SUPABASE_ANON_KEY is not set. Please provide it in your environment configuration.');
}

const authStorage = typeof window !== 'undefined' ? window.localStorage : undefined;

const withTableSession: typeof fetch = (input, init = {}) => {
  const headers = new Headers(init?.headers ?? {});
  if (typeof window !== 'undefined') {
    const sessionId = getTableSessionHeader();
    if (sessionId) {
      headers.set('x-icupa-session', sessionId);
    }
  }
  return fetch(input, { ...init, headers });
};

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_ANON_KEY, {
  auth: {
    storage: authStorage,
    persistSession: true,
    autoRefreshToken: true,
  },
  global: {
    fetch: withTableSession,
  },
});
