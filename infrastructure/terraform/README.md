# Terraform Modules

Use this folder for shared Terraform modules and environment-specific stacks.
Keep state files outside of the repository and document backend configuration
inside the respective module directories.
